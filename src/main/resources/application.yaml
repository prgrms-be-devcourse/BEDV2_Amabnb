spring:
  h2:
    console:
      enabled: true
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
  security:
    user:
      name: user
      password: 04a13c19-7ffb-490f-9b3b-ec312e12d3cf
  jpa:
    database: H2
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
    hibernate:
      ddl-auto: create

jwt:
  issuer:
  secret-key:
  access-token:
    expire-length:
  refresh-token:
    expire-length: